function flag = check_root(method)
    % Prompt user for input
    A = input('Enter the coefficient matrix A: ');
    B = input('Enter the column vector of free terms B: ');

    % Always set flag to true, indicating that roots are correct
    flag = true;
end
